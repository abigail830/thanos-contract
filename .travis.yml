---

env:
  - CODECOV_TOKEN="ad723cb3-3c21-446c-9630-3c3cf4410b7f"

matrix:
  include:
  -
    before_script:
    - "cd MockServer"
    jdk: openjdk8
    language: java
    script: "mvn clean package"
  -
    before_script:
    - "cd ContractService"
    jdk: openjdk8
    language: java
    script: "mvn clean package"
  -
    before_script:
    - "cd codegenerator"
    jdk: openjdk8
    language: java
    script: "mvn clean package"


script: "mvn cobertura:cobertura"

after_success:
- bash <(curl -s https://codecov.io/bash)